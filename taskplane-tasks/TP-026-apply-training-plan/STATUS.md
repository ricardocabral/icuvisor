# TP-026-apply-training-plan: TP-026-apply-training-plan — Status

**Current Step:** Step 5: Verify
**Status:** 🟡 In Progress
**Last Updated:** 2026-05-13
**Review Level:** 0
**Review Counter:** 0
**Iteration:** 3
**Size:** M

---

### Step 1: Schema

**Status:** ✅ Complete

- [x] Inputs: `plan_id`, `start_date` (anchor; plan dates are relative), optional `dry_run: bool` (default `true`), `conflict_policy` (`skip_existing` | `replace_existing` — `replace_existing` requires `CanDelete()`)
- [x] Plan content fetched server-side, never passed in by the LLM
- [x] Default `dry_run: true` is the safety net even in `safe` mode — the LLM must explicitly set `dry_run: false` to mutate

---

### Step 2: Dry-run path

**Status:** ✅ Complete

- [x] Returns the proposed event list with conflict markers; no upstream writes
- [x] Per-day shape: `{date, workout_id, conflicts: [{event_id, reason}]}`

---

### Step 3: Apply path

**Status:** ✅ Complete

- [x] `dry_run: false` + `conflict_policy: skip_existing` → create events only on conflict-free days; skipped days listed in `_meta.skipped`
- [x] `dry_run: false` + `conflict_policy: replace_existing` → registered only when `CanDelete()` (this argument is rejected at the validation layer otherwise); deletes the conflicting event then creates the new one
- [x] Use TP-020's `add_or_update_event` internals; do not duplicate event-build logic

---

### Step 4: Response shape

**Status:** ✅ Complete

- [x] `_meta.created_count`, `_meta.skipped`, `_meta.replaced` (when applicable), `_meta.delete_mode`

---

### Step 5: Verify

**Status:** 🟨 In Progress

- [x] `make test`, `make build`, `make lint`, `go test -race ./...`
- [ ] Manual smoke against the test athlete: dry-run, skip-existing apply, replace-existing apply (in `full`)

---

## Reviews

| #   | Type | Step | Verdict | File |
| --- | ---- | ---- | ------- | ---- |

---

## Discoveries

| Discovery | Disposition | Location |
| --------- | ----------- | -------- |

---

## Execution Log

| Timestamp        | Action                 | Outcome                                                                                                                                                                                                                                                                                                    |
| ---------------- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-05-13       | Task staged            | STATUS.md auto-generated by task-runner                                                                                                                                                                                                                                                                    |
| 2026-05-13 21:24 | Task started           | Runtime V2 lane-runner execution                                                                                                                                                                                                                                                                           |
| 2026-05-13 21:24 | Step 1 started         | Schema                                                                                                                                                                                                                                                                                                     |
| 2026-05-13       | Automated verification | PASS: `make test`, `make build`, `make lint`, and `go test -race ./...` all completed successfully.                                                                                                                                                                                                        |
| 2026-05-13       | Manual smoke blocked   | `/Users/jusbrasil/prj/icuvisor/.env-dev` exists and exposes key names for API key, athlete ID, and `ICUVISOR_DELETE_MODE`, but it was explicitly approved only for TP-022 smoke; no TP-026 plan fixture ID or approval to perform full-mode event replacement was found. No live writes/deletes attempted. |
| 2026-05-13 21:47 | Agent escalate         | TP-026 Step 5 manual smoke is blocked. Automated verification passed (`make test`, `make build`, `make lint`, `go test -race ./...`) and README/CHANGELOG/schema snapshot are updated. The remaining che                                                                                                   |
| 2026-05-13 21:47 | Worker iter 1          | done in 1388s, tools: 127                                                                                                                                                                                                                                                                                  |
| 2026-05-13       | Steering received      | Operator directed that TP-026 live manual smoke must not run yet: do not source credentials, do not use `.env-dev`, and do not perform live writes/deletes until TP-026 scope is explicitly approved with a disposable `plan_id` fixture.                                                                  |
| 2026-05-13       | Manual smoke plan      | Ready-to-run plan documented in Notes; manual-smoke checkbox intentionally remains unchecked pending operator approval/scope.                                                                                                                                                                              |
| 2026-05-13 21:49 | ⚠️ Steering | Do not run TP-026 live manual smoke yet. Prior full-mode approval was scoped to earlier smoke tests, not bulk training-plan application with replace-existing calendar deletes. Do not source credential |
| 2026-05-13 21:49 | Worker iter 2 | done in 163s, tools: 9 |
| 2026-05-13 21:49 | No progress | Iteration 2: 0 new checkboxes (1/3 stall limit) |
| 2026-05-13       | Agent escalate         | Reconfirmed Step 5 manual smoke remains blocked by explicit operator steering forbidding credentials/live writes or deletes until TP-026 receives an approved disposable `plan_id` fixture; requested approval, fixture, or waiver to substitute offline coverage. |

---

## Blockers

| Date       | Blocker                                                                                                                                                                                                                                        | Attempts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-05-13 | Step 5 manual smoke requires a live test-athlete training-plan fixture (`plan_id` with relative day workouts) and explicit approval for the full-mode replace-existing apply, because it deletes calendar events before creating replacements. | Checked current environment and repo config without printing secrets; no `INTERVALS_ICU_*`/`ICUVISOR_*` credentials are set in the worktree environment. Checked only key names in `/Users/jusbrasil/prj/icuvisor/.env-dev`; it contains API key, athlete ID, and `ICUVISOR_DELETE_MODE`, but comments identify approval for TP-022 only and there is no TP-026 plan ID fixture. Automated verification passed, and unit tests cover dry-run, skip-existing apply, and full-mode replacement behavior offline. |

---

## Notes

- README, CHANGELOG, and schema snapshot have been updated for `apply_training_plan`; automated verification passed after those edits.
- Ready-to-run TP-026 manual smoke plan, to execute only after explicit operator approval for TP-026 bulk training-plan application:
  1. Use only `/Users/jusbrasil/prj/icuvisor/.env-dev` for credentials after approval; do not source or inspect it before approval.
  2. Require a maintainer-provided or maintainer-approved disposable training-plan `plan_id` whose workouts target isolated future dates on the test athlete.
  3. Run `apply_training_plan` with `dry_run: true` first and verify proposed events, conflict markers, `_meta.created_count == 0`, and no upstream writes.
  4. Run `dry_run: false` with `conflict_policy: skip_existing` only against isolated future dates; verify created events and `_meta.skipped` for pre-existing conflicts without deleting anything.
  5. For `replace_existing`, enable/use `ICUVISOR_DELETE_MODE=full` only under the approved smoke scope and replace only same-run disposable conflict events created for this smoke test.
  6. Verify cleanup/restoration: remove disposable events created during the smoke or restore the test athlete calendar to its prior state; record event IDs, counts, and any cleanup result before checking the manual-smoke box.
