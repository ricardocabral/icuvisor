# R020 Code Review — Step 7: Replace duplicate prose

Decision: APPROVE

I reviewed `git diff d306812..HEAD`, read the changed files, and grepped for remaining hardcoded tool-list/count/chip prose in `README.md` and `web/layouts`.

## Findings

No blocking findings.

## Notes

- `README.md` now replaces the long hand-maintained MCP tool list with a short paragraph and the canonical `https://icuvisor.app/reference/tools/` link.
- The stale `~25 MCP tools` claims in both `README.md` and `web/layouts/index.html` are gone; the landing page keeps the generated `tool-chips.html` partial.
- `web/README.md` now documents that `web/data/tools.json` is generated by `make docs-tools` / `cmd/gendocs` and should not be hand-edited.
- I did not run the full test suite; this review was limited to the Step 7 documentation/prose changes.
