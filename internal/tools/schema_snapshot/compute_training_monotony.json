{
  "additionalProperties": false,
  "properties": {
    "athlete_id": {
      "description": "Target athlete; defaults to selected athlete in coach mode, or the only athlete otherwise. Format: i12345 or 12345.",
      "type": "string"
    },
    "end_date": {
      "description": "Inclusive athlete-local end date, YYYY-MM-DD; at most 31 expected dates.",
      "type": "string"
    },
    "include_full": {
      "default": false,
      "description": "When true, include validated daily evidence in series.",
      "type": "boolean"
    },
    "start_date": {
      "description": "Inclusive athlete-local start date, YYYY-MM-DD.",
      "type": "string"
    }
  },
  "required": [
    "start_date",
    "end_date"
  ],
  "type": "object"
}
