{
  "additionalProperties": false,
  "properties": {
    "athlete_id": {
      "description": "Target athlete; defaults to selected athlete in coach mode, or the only athlete otherwise. Format: i12345 or 12345.",
      "type": "string"
    },
    "end_date": {
      "description": "local end date as YYYY-MM-DD in the athlete timezone.",
      "type": "string"
    },
    "include_full": {
      "default": false,
      "description": "When true, include raw upstream summary rows.",
      "type": "boolean"
    },
    "include_per_sport_load_trends": {
      "default": false,
      "description": "When true, include computed per-sport CTL/ATL/TSB-style load trend estimates for running, cycling, swimming, and other from athlete-summary byCategory training_load. Existing combined fitness rows remain unchanged.",
      "type": "boolean"
    },
    "start_date": {
      "description": "local start date for fitness rows as YYYY-MM-DD in the athlete timezone.",
      "type": "string"
    }
  },
  "required": [
    "start_date",
    "end_date"
  ],
  "type": "object"
}
